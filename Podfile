# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'mobillium' do
  # Comment the next line if you don't want to use dynamic frameworks

  use_frameworks!

  # Pods for mobillium
  pod 'Alamofire'
  pod 'Kingfisher', '~> 5.0'

  target 'mobilliumTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mobilliumUITests' do
    # Pods for testing
  end

end
